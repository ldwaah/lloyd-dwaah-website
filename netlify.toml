[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so client-side routing / anchors always resolve
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"

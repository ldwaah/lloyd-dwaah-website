/** Shared motion tokens — Lando-inspired easing, Lloyd brand timing. */
export const easeOut = [0.22, 1, 0.36, 1];

export const sectionReveal = {
  duration: 0.95,
  revealDelay: 0.22,
  y: 28,
};

export const parallax = {
  /** Background layers move slower than foreground. */
  slow: [0, 1],
  slowY: [0, -120],
  mediumY: [0, -64],
  fastY: [0, -32],
};
